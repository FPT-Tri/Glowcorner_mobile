<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity android:name=".CartActivity" />
        <activity android:name=".CheckoutActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ManagerHomeActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".RoutineDetailActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".StaffHomeActivity" />
        <activity android:name=".SuccessActivity" />


        <activity android:name=".manager.ManagerUsersActivity" />
        <activity android:name=".manager.ManagerEditUserActivity" />
        <activity android:name=".manager.ManagerCustomersActivity" />
        <activity android:name=".manager.ManagerStaffsActivity" />

        <activity android:name=".manager.ManagerOrdersActivity" />

        <activity android:name=".manager.ManagerPromotionsActivity" />
        <activity android:name=".manager.ManagerRoutinesActivity" />

        <activity android:name=".manager.ManagerProductsActivity" />
        <activity android:name=".manager.EditProductActivity" />
        <activity android:name=".manager.CreateProductActivity" />



        <activity android:name=".staff.StaffSkinQuizActivity" />


        <activity android:name=".SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
